node_modules/
dist/
*.log
.DS_Store

# Personal Bloom data — never commit user content.
# The recovery/*.sh tooling stays tracked; only the data snapshots are ignored.
recovery/bloom-data-*.json
recovery/*before-recovery.json
